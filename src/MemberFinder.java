public class MemberFinder {


    /**
     * Produces a file containing 2 columns;
     * (1) Name of the bookee
     * (2) Whether they are a member or not
     */
    public void produceBookingsList()
    {
        //

        /*
        Read the members csv
        get the names from the members csv

        read the bookings csv
        get the names from the bookings csv

        read the extra members csv
        get the names from the extra members csv

        merge members names with extra members names
         */
    }
}
